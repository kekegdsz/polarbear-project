# Vue Router 纯 Nginx 配置（无 PHP）
# 放入宝塔 网站 → 设置 → 伪静态

# 使用 ^~ 优先匹配，避免与敏感文件规则冲突
location ^~ /changelog {
    rewrite ^ /index.html last;
}
location ^~ /download {
    rewrite ^ /index.html last;
}
location / {
    try_files $uri $uri/ /index.html;
}
